import { RealsearchPipe } from './realsearch.pipe';

describe('RealsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new RealsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
